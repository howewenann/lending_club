from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Analysis on loan default using lending club data',
    author='Nicholas',
    license='',
)
